class FileIO : public CCObject
{
public:
	static FileIO* create();
	virtual bool init();
	ByteBuffer* getFileData(const char* filename); 
	Texture2D* openImage(const char* jpgFile,const char* maskFile);
};